# 本文件已改为“说明文件”，不再是依赖清单的真相来源。
#
# 真相来源：
#   - pyproject.toml  → 直接依赖（openai / openai-agents / python-dotenv + dev 组 pytest / ruff）
#   - uv.lock         → 完整解析后的锁文件（含传递依赖）
#
# 环境安装（推荐，用 uv）：
#   uv sync                     # 按 uv.lock 安装到 .venv
#   uv sync --no-dev            # 只要运行依赖，不装 pytest / ruff
#
# 兼容旧方式的等价命令：
#   uv venv .venv
#   uv pip install --python .venv/bin/python openai-agents python-dotenv
#
# Python 版本要求：≥3.12（main.py 的 asyncio.timeout 需 3.11+，
# eval_cases.py 的 Path.is_junction() 需 3.12+）。本机实际使用 3.14.4。
#
# 历史备注：本文件此前钉过 openai==3.8.0 / openai-agents==0.22.0 /
# python-dotenv==1.2.3 / requests==2.34.2，现已全部迁移到 pyproject.toml。
# requests 只在旧的 python-base/ 练习里用到，该目录已移出仓库。
