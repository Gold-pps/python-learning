# 工程化改造记录（G1 · 第 13~15 周）

> 用途：第二阶段的"技术债台账"。**改一条记一条**，不攒到周末补写——补写出来的复盘通常只剩下结论，丢掉了最有价值的现象和证据。
> 每条固定四段：**现象 → 根因 → 改法 → 验证**，末尾附"影响面"（这次改动碰了谁、留下了什么）。
> 对照清单：`学习评审报告.md` 第 11.2 节。

## 第 13 周

### T1 `search_notes` 不再全量读入大文件

| 项 | 内容 |
| --- | --- |
| 现象 | `read_note` 有 1MB 上限，`search_notes` 没有任何大小限制：搜索会对每个 `.md` 直接 `read_text()` 全量读入。 |
| 根因 | 大小检查缺位，且**检查的位置**比"有没有检查"更关键——必须放在 `read_text()` 之前，否则"检查"本身就已经把文件读进内存了。这是一个典型的"看起来是缺功能、其实是缺顺序"的问题。 |
| 改法 | `tools.py / search_notes`：取 `st_size` 超过 `MAX_FILE_BYTES` 就跳过并计数；两个工具对同一个限制采取了不同策略——`read_note` **报错**（用户要的就是那一个文件，静默返回空内容更糟），`search_notes` **跳过并计数**（一个坏文件不该让整次搜索失败）。计数回传字段 `skipped_large_files`，防止模型把"没搜到"误当成"笔记里没有"。 |
| 验证 | 造一个 1MB+ 的探针文件，里面埋独有关键词 `UNIQUE-BIG-KEYWORD`：修复后返回 `{'ok': True, 'matches': [], 'skipped_large_files': 1}`（关键词搜不到，但明确告知跳过了 1 个文件）；同期正常搜索 `护栏` 返回 5 条、`skipped_large_files: 0`，未受影响。`eval_cases.py --unit-only` 仍 12/12。 |
| 影响面 | 工具返回值新增字段，`notes-qa/项目设计.md` 的工具输出说明待同步（排入第 15 周"文档收口"）。 |
| 决策记录 | 本条的**固定单元用例**留到第 14 周：那一周要把手写 `UNIT_CASES` 整体迁到 pytest，用例数量会变，此刻插一条会让"12 条"这个口径在第 13、14 周之间来回改。临时验证已用一次性脚本做掉。 |

### T2 评估探针不再残留（`os.rmdir` → `os.unlink`）

| 项 | 内容 |
| --- | --- |
| 现象 | 每次跑 `eval_cases.py --unit-only`，仓库里多出一个**断链** `agent-learning/_probe_linkdir.md`（指向已删除的 `_probe_linkdir_target`）。测试**全部通过**，却污染了工作区——这类"绿灯下的脏数据"最容易长期存活。 |
| 根因 | U6 的 `finally` 里用 `os.rmdir(link)` 删除**符号链接**：`os.rmdir` 只能删目录本身，符号链接对它来说不是目录，于是抛 `NotADirectoryError`；而 `except OSError: pass` 把这声报错吞掉了，最终表现为"测试全绿 + 仓库多一个断链"。原注释只考虑了"junction 要 rmdir"，漏了"符号链接必须 unlink"——**分支写少了，而且少的那一支恰好是当前平台走的那一支**。 |
| 改法 | 拆成两个分支：`is_symlink()` → `os.unlink()`；`is_junction()` → `os.rmdir()`。顺序不能反：Windows 上 junction 的 `is_symlink()` 为 False，Linux 上 `is_junction()` 恒为 False，两者互不干扰。 |
| 验证 | ① 最小复现已证明 `os.rmdir` 抛 `NotADirectoryError`、`os.unlink` 成功；② 修复后重跑 `--unit-only` 得到 12/12，且 `ls agent-learning/_probe_*` 与仓库根均无残留。 |
| 影响面 | 遗留风险：`finally` 里仍然 `except OSError: pass`，清理失败依然**只留痕迹、不报警**。根治办法是第 14 周迁到 pytest 时把探针挪进 `tmp_path`——临时目录本身由 pytest 兜底回收，不再依赖手写清理逻辑的正确性。 |

最小复现（不依赖任何项目代码，可换机重放）：

```python
import os, pathlib
link = pathlib.Path("link")                    # 指向某个目录的符号链接
print(link.is_symlink(), link.is_junction())   # True False
os.rmdir(link)                                 # NotADirectoryError: [Errno 20] Not a directory
```

### T3 `main.py` 的会话历史收敛到常数规模

| 项 | 内容 |
| --- | --- |
| 现象 | 交互式问答里 `ask()` 直接 `return result.to_input_list()`，history 只增不减：每一轮的输入 = 全部历史 + 本轮问题。会话越长，每问一句要重发的输入越多，同时 history 列表本身占的内存也在涨。 |
| 根因 | `to_input_list()` 会把本轮**工具返回的原文**一起写进历史——`read_note` 一次最多 200 行，一轮能塞进几千字符，于是输入规模随轮数**线性增长**。缺少的不是某个功能，而是"历史这个数据结构的容量策略"：它被当成只写不读的内存用，没人负责收敛。 |
| 改法 | `main.py` 新增 `MAX_HISTORY_TURNS = 8` / `MAX_HISTORY_ITEM_CHARS = 2000` 两个常量，以及 `_truncate_text` / `_trim_item` / `_keep_recent_turns` / `prepare_history` 四个函数；收敛动作只放在 `ask()` 的**写入侧**一处：`return result.final_output, prepare_history(result.to_input_list())`。三个关键设计见下。 |
| 验证 | 见下方"实测数据"。另：`--unit-only` 仍 12/12，无 `_probe_*` 残留。 |
| 影响面 | `MAX_HISTORY_TURNS = 8` 是**初值**，等第 22 周拿到真实使用日志后再按"够不够用 / 贵不贵"调；本轮只做了字符数代理指标，真实 token 曲线用 `notes-qa/exp_history_tokens.py --no-trim` 对照跑（见下）。 |

**实测脚本**：`notes-qa/exp_history_tokens.py`（2026-09-18 新增）。两臂只差一处——
`--no-trim` 时把 `result.to_input_list()` 原样存下（等价于修复前那一行），其余路径完全相同，
所以曲线可比。脚本会打印每轮 `输入 / 缓存命中 / 输出 / history 体积 / 耗时`，并算出高峰价与低谷价的
成本估算，最后输出可直接贴进笔记的表格行。

跑之前先定判据（**先定判据再跑，避免看到数字再找解释**）：

1. 修复后：第 9 轮起输入 token 应基本持平（上限 8 轮）；若仍持续增长，说明有内容没被截断；
2. 修复前：输入 token 应接近线性增长，末轮约为首轮的数倍；
3. 两臂的第 1 轮**不会完全相同**——冒烟测试的实测值是 `18,321`（收敛臂）对 `14,834`（不收敛臂），差 23%。
   即使 `temperature=0`，模型这一轮调几次工具、每次读多少行仍会变，所以第 1 轮只能"量级相近"。
   **判据要看趋势（线性 vs 持平），不要逐点比对**；想更稳就把同一臂跑 2~3 次看趋势是否一致；
4. 额外看**缓存命中**那一列：截断让窗口滑动，前缀不再稳定，命中率可能下降。
   若"省下的 token"被"降低的缓存命中率"部分抵消，这条曲线的结论就要写成
   "token 线性增长被止住，但单位成本未必同比例下降"——这正是第 7 周笔记里缓存价那条经验的延伸。

**冒烟测试记录（2026-09-18，各 1 轮，用于验证脚本本身）**：

| 臂 | 输入 | 缓存命中 | 输出 | history | 耗时 | 低谷价 |
| --- | --- | --- | --- | --- | --- | --- |
| 收敛 | 18,321 | 14,080 | 2,465 | 25 条 / 13,816 字符 | 13.7s | ≈0.015 元 |
| 不收敛 | 14,834 | 11,136 | 1,959 | 21 条 / 14,641 字符 | 12.0s | ≈0.013 元 |

两条附带结论：① 单轮输入就有 1.5~1.8 万 token——工具调用循环（search → read → 回答要 3~4 次请求，
每次重发全文）才是本项目最大的输入来源，历史累积是叠加在这之上的；② 单轮成本约 1.5 分钱，
8 轮 × 两臂合计预计 **0.3~0.6 元**（低谷价），比最初"不到一毛"的估计高一档，按实际数字记账。

**真实 token 曲线结论（2026-09-18，两臂各 8 轮，数据见 `第13周笔记.md` 第 3.4 节）——本条验证要改写**：

| | 收敛臂 | 不收敛臂 |
| --- | --- | --- |
| 输入 token 合计 / API 请求数 / 缓存命中率 | 389,573 / 25 次 / **82%** | 503,415 / 29 次 / **95%** |
| 低谷价 | 0.151 元 | 0.080 元 |

1. **修复在 8 轮规模下几乎没生效**：`MAX_HISTORY_TURNS=8` 正好被 8 轮用满、从未触发；
   单条 2000 字符上限也没触发（两臂 history 体积 79,712 vs 83,695 字符，条目平均约 800 字符）。
   → 原先"字符数验证通过"只证明了逻辑正确，**没有证明收益存在**。
2. **主导变量是"该轮工具往返次数"，不是历史长度**：请求次数实测在 1~7 次波动，
   单轮输入 = 请求次数 × 每次重发的上下文，第 4 轮 7 次请求直接顶到 118,640 token。
3. **噪声大于效应**：第 1 轮两臂代码路径完全相同，却跑出 7,659 vs 15,297（2 倍）→ n=1 不能比较两臂。
4. **新假设（待验证）**：截断改变历史内容 → 下一轮前缀与已缓存前缀不一致 → 缓存命中率下降（82% vs 95%）。
   缓存命中价只有未命中的约 1/50，**破坏缓存可能比省下的 token 更贵**。
5. **参数要按真实规模重定**：真实使用是"一次提问 1~7 次工具往返、单条返回约 800 字符"，
   所以 `8 轮 / 2000 字符` 属于拍脑袋初值；要么调到会被触发的量级，要么用 15~20 轮重测。

**这条经验的普适版本**：一个修复"逻辑正确"和"有收益"是两件事，**验证必须用真实规模的负载**。
合成负载（6 KB/条、20 轮）证明了正确性，真实负载（800 字符/条、8 轮）证明了它没用武之地——
两个结论都对，只有后者能决定要不要保留这个复杂度。

三个关键设计（这部分比代码本身值钱）：

1. **必须从 `user` 消息处切开**。一条 user 消息 + 它引发的 `function_call` / `function_call_output` / 最终回答才是完整一轮。从中间切会留下"没有对应 `function_call` 的 `function_call_output`"（孤儿工具结果），发给 API 会被直接拒掉——这个坑只在被截断的那一次才暴露，很容易漏到线上。
2. **收敛只做在写入侧一处**。history 是"只在这里写、只被下一轮读"的数据，在写入口收敛一次，就不需要在每个用到 history 的地方各截一遍；两处各截一次的逻辑迟早会分叉。
3. **截断返回副本**（`_trim_item` 里 `dict(item)` + 重建 list），不改调用方给的原对象。改历史副本和改工具返回值是两回事：工具返回值是"这一轮模型要看的资料"（不能动），历史副本只是"下一轮还需要记住的摘要"。

实测数据（合成 20 轮会话，每轮工具返回约 6 KB，`json.dumps` 长度求和）：

| 轮数 | 未收敛 | 已收敛 |
| --- | --- | --- |
| 4 | 26,384 | 10,280 |
| 8 | 52,768 | 20,560 |
| 12 | 79,167 | 20,575 |
| 24 | 158,379 | 20,600 |
| 48 | 316,803 | 20,600 |

未收敛时线性增长，收敛后**钉在 2 万字符左右**（≈8 轮 × 每轮 2 个被截到 2000 字符的条目）。
20 轮长会话单独测：125,140 字符 → 17,992 字符。
未触上限的短会话逐条等价（`prepare_history(small) == small` 为 True），说明截断逻辑不会"顺手改动"正常历史。

**待你补的实测**（需要联网、烧 token，沙箱里跑不了）：同一组长会话分别用旧版/新版跑一遍，
把 `[用量] 输入 tokens` 随轮数的曲线记进 `第13周笔记.md`。字符数只是代理指标，token 才是账单。

最小复现方法（无需联网，任何人可重放）：

```python
import json, main

def make_turn(i, tool_chars=6000):
    return [
        {"role": "user", "content": f"第 {i} 问"},
        {"type": "function_call", "name": "read_note", "call_id": f"c{i}", "arguments": "{}"},
        {"type": "function_call_output", "call_id": f"c{i}",
         "output": json.dumps({"ok": True, "content": "正" * tool_chars}, ensure_ascii=False)},
        {"role": "assistant", "content": f"第 {i} 答"},
    ]

history = [it for i in range(20) for it in make_turn(i)]
print(len(history), "->", len(main.prepare_history(history)))
```

**决策记录**

- 常量放 `main.py` 而非 `config.py`：`config.py` 管"怎么连模型"（地址、超时、重试），这两条管"这个命令行会话怎么记事儿"，与 `MAX_QUESTION_LEN` 同类。
- 没有做"早期对话摘要"（用模型压缩老历史）：那是每轮都要多花一次模型调用和一次失败可能性的设计，等 T3 的简单上限被真实使用打脸（比如第 22 周发现"忘了第 2 轮说过什么"）再加，先上便宜的。
- 截断用字符数而不是 token 数：`tiktoken` 是 OpenAI 的分词器，对 DeepSeek 不精确，为估算再引一个依赖不划算。字符数是保守代理，量级判断够用。
- 截断痕迹写进内容本身（`…[历史截断，原文 N 字符]`）：模型能看见"这里被省略了"，比静默丢字更不容易产生"我被喂了完整资料"的错觉。

### T4a 清理残留 `agent/` 目录（第二份 `.env`）

| 项 | 内容 |
| --- | --- |
| 现象 | 仓库根有一个 `agent/` 目录，里面有 `.env`（52 字节）与两个 9 月 8 日的 `.pyc`。它是目录改名前的历史遗留，不在任何代码的引用范围内。 |
| 根因 | 早期把练习脚本放在 `agent/` 下，后来改名 `agent-learning/`，旧目录只删了代码没删配置目录。`.gitignore` 里还留着 `agent/.env`、`agent学习/.env` 两条**已不存在的路径**，说明当时的清理动作做了一半。 |
| 改法 | ① 先做三项只读确认：`git ls-files agent` 为空（未被跟踪）、`cmp -s agent/.env agent-learning/.env` 完全相同（删了不丢信息）、全仓 grep 无任何路径引用；② 再确认**全历史**没有提交过 `.env`（`git log --all --name-only --diff-filter=A`）——这一步决定了性质是"清理副本"还是"密钥泄露需换 Key"，结论是前者；③ 把整个目录 `mv` 到 `/tmp/python-learning-trash-20260918/`（可恢复，重启后自动清空），而不是直接删除；④ `.gitignore` 删掉两条历史路径——全局 `.env` 规则已经覆盖一切嵌套位置，再单列 `agent-learning/.env` 属于冗余，冗余规则将来会漂移。 |
| 验证 | 目录已移出（`ls` 报不存在），仓库根恢复干净；被移走的 `.env` 仍可 `wc -c` 读到 52 字节。 |
| 影响面 | 无。这是唯一一条"删完就没有反面"的改动。 |

**顺带验证掉的一个怀疑**：`.gitignore` 首行带 UTF-8 BOM（`efbbbf`）。我怀疑它会让第一条规则 `__pycache__/` 失效，实测 `git check-ignore -v` 命中 `.gitignore:1:__pycache__/`——当前 git 版本会剥掉 BOM，规则生效，**不是 bug**。如果没验证就"顺手修 BOM"，就是在制造一次没有收益的风险。

### T4b `~/.codex/config.toml` 的明文 API Key 改环境变量注入

| 项 | 内容 |
| --- | --- |
| 现象 | Codex 的全局配置里，自定义 provider 的凭据是明文：`experimental_bearer_token = "sk-…"`。这个文件是会被分享、截图、贴进 issue 的典型对象。 |
| 根因 | 配置里同时用了"把密钥写死在配置中"的字段（`experimental_bearer_token`）。Codex 的 `ModelProviderInfo` 还有另一个字段 `env_key`，语义是"凭据从环境变量取"，正是为这种情况准备的（字段来源：本机 codex 二进制内的配置结构，并用 `codex exec --strict-config` 验证过它被识别——严格模式只对 `preferred_auth_method` 报未知字段，没报 `env_key`）。 |
| 改法 | ① 备份原配置到 `/tmp/python-learning-trash-20260918/config.toml.bak`（600）；② `sed` 删掉 bearer 行、在 `wire_api` 后加 `env_key = "DEEPSEEK_API_KEY"`（用 `sed` 而不是编辑器补丁，是为了让密钥值不出现在任何命令、日志或对话里）；③ 密钥本体写进 `~/.config/deepseek/api.env`（600），由 `~/.bashrc`、`~/.profile` 各引一次；④ 因为 VS Code 是 **systemd 直接拉起**的（`ps` 显示父进程是 systemd，`/proc/<pid>/environ` 里没有该变量），shell rc 覆盖不到 GUI 启动路径，所以再加 `~/.config/environment.d/deepseek.conf`（600），并用 `systemctl --user import-environment` 做即时注入。 |
| 验证 | 见下方"实测矩阵"。真实调用共两次（隔离环境一次 + 真实配置一次），各约 1.5k tokens。 |
| 影响面 | ⚠️ **这是本周唯一有使用代价的改动**：环境变量缺失时 Codex 会直接失败。应急回滚一行：`cp /tmp/python-learning-trash-20260918/config.toml.bak ~/.codex/config.toml`。 |

实测矩阵（全部为实测，非推断）：

| 场景 | 结果 |
| --- | --- |
| 隔离环境：`env_key` + 有环境变量 | ✅ 正常回答，1616 tokens |
| 隔离环境：`env_key` + 无环境变量 | ❌ `Missing environment variable: DEEPSEEK_API_KEY.`——**失败提示明确**，不会让人猜 |
| 隔离环境：无 `env_key` + 有 `auth.json`（`codex login --with-api-key` 生成） | ❌ 401 未授权——**自定义 provider 不走 auth.json**，这条路被排除 |
| 真实配置 + 登录 shell | ✅ 正常回答，1463 tokens |
| `bash -lc` / `bash -ic` 里读环境变量 | ✅ 长度 35 |
| 非交互非登录的 `bash -c` | ❌ 取不到（0）——已知边界，脚本里调用 Codex 需自行 export |
| `systemd` generator 解析 `environment.d` | ✅ 能解析出 `DEEPSEEK_API_KEY`（键名验证） |
| 重新登录后 GUI 路径是否生效 | ⏳ **未验证**（需要重新登录才能测），见下方待办 |

**未验证项的诚实标注**：`environment.d` 真正的生效时机是下次用户会话启动。当前 VS Code 进程（已经是 systemd 拉起的、环境里没有该变量的那个）在重启前仍然拿不到变量。已用 `systemctl --user import-environment` 让会话管理器持有该变量，但**已经运行的进程不会被追溯修改**——所以下一步是重启 VS Code，并在重启后按下面的方式确认。

**下次开机（或重新登录）后的确认动作**：

```bash
systemctl --user show-environment | grep DEEPSEEK_API_KEY   # 有输出 = 会话环境已生效
codex doctor --no-color | grep -A2 "auth"                   # 应显示 ✓ auth
```

若 VS Code 里仍然报缺变量：从终端启动一次（`code /home/gold-pps/github-repositories/python-learning`），
终端启动会继承登录 shell 的环境；或临时回滚到 T4b 的备份。

**顺带发现（未处理，交给你决定）**：真实配置里的 `preferred_auth_method = "apikey"` 被 `codex exec --strict-config` 报为**未知字段**（`unknown configuration field`），说明它不被当前 0.154.0 识别、目前是空转。本轮没有动它——未知字段可能在新版（0.155.0 已可更新）里重新变成有效项，删不删都需要你先确认意图。

### T5 密钥泄露面盘点（T4b 之后复查，2026-09-18）

| 项 | 内容 |
| --- | --- |
| 现象 | T4b 把密钥从 `config.toml` 移走后，终端验证时把 `systemctl --user show-environment` 的原始输出直接贴进了对话——里面带着完整明文。 |
| 根因 | 把"密钥从配置文件里挪走"当成了"密钥就不再外泄"。实际上一个 Key 的落点至少有三类：① **对话记录**（每轮作为上下文发给模型服务，同时落进 `~/.codex/sessions/**/rollout-*.jsonl`、`thread_history`、`logs` sqlite）；② **进程环境**（`env`、`/proc/<pid>/environ`、子进程、构建日志）；③ **派生缓存**（Codex 的 `shell_snapshots/*.sh` 把 shell 环境以 `declare -x KEY="…"` 的形式整份抄下来）。这次三类都命中了。 |
| 改法 | 真正的补救只有一个：**轮换密钥**，因为已外泄的值在轮换前始终有效。清理本地文件只是辅助，且优先级低于轮换。 |
| 验证 | 落点扫描（只列路径，不打印值）：`grep -rla <前缀> ~/.codex/` 命中 10 个文件（含本会话 rollout、`thread_history_1.sqlite`、`logs_2.sqlite-wal`、一个 shell 快照）。**对照实验**：`shell_snapshots/` 里会话启动时的旧快照（12:49）不含该 Key，我加完 `.bashrc`/`.profile` 后的新快照（13:27）含——即**是我这次改动把密钥带进了快照**，证据确凿。 |
| 影响面 | 已确认**权限不构成额外风险**：`~/.codex` 目录是 700，快照文件虽然是 664，组和其他人根本进不去目录，实际只有本人（和 root）可读。真风险集中在对话记录与"把输出贴到哪"。 |

**一条重要的否定结论（避免以后重复试）**：想用配置让子 shell 看不到这个凭据，**两条路都试过、都不行**：

| 方案 | 实测 |
| --- | --- |
| 对照组：不加任何策略 | `env` 里能看到 `DEEPSEEK_API_KEY`（1 处） |
| `[shell_environment_policy] exclude = ["DEEPSEEK_API_KEY"]` | 仍能看到（1 处）——**无效** |
| `[shell_environment_policy] include_only = [...白名单...]` | 仍能看到（1 处）——**无效**（且两种写法都通过了 `--strict-config`，说明字段名没错，是机制不覆盖这里） |

结论：在这个版本里，**"用环境变量注入"必然意味着"凭据对 Codex 启动的子 shell 与快照可见"**。这不是配置能修的，只能靠轮换周期与"不把原始环境输出贴到任何地方"来管。

**决策记录（2026-09-18）**：用户决定**不轮换**该 Key，接受已知风险。台账按此记录，不再列为待办；
下列轮换步骤保留供将来参考（例如真出现异常调用或要把仓库/配置分享出去时）。**未轮换 = 上述落点里的值仍然有效**，
这一点在判断"某天账单异常"时是前提条件。

轮换步骤（备用）：

1. DeepSeek 平台 → API Keys → 新建一个 Key，删除旧的（这一条做完，本文档里所有已外泄的值立刻失效）；
2. 用下面这段交互式命令更新三处副本（新 Key 不进 shell 历史、不进对话记录）：

```bash
read -rsp "粘贴新的 DeepSeek Key: " NEW_KEY && echo
printf 'DEEPSEEK_API_KEY=%s\n' "$NEW_KEY" > ~/.config/deepseek/api.env
printf 'DEEPSEEK_API_KEY=%s\n' "$NEW_KEY" > ~/.config/environment.d/deepseek.conf
printf 'DEEPSEEK_API_KEY=%s\n' "$NEW_KEY" > ~/github-repositories/python-learning/agent-learning/.env
chmod 600 ~/.config/deepseek/api.env ~/.config/environment.d/deepseek.conf ~/github-repositories/python-learning/agent-learning/.env
systemctl --user import-environment DEEPSEEK_API_KEY
unset NEW_KEY
```

3. 重启 VS Code 与终端，再跑一次 `codex doctor --no-color | grep -A2 auth` 确认 `✓ auth`；
4. 旧快照可以顺手清掉（`~/.codex/shell_snapshots/`），但**别把它当补救手段**——轮换前清文件是把扫地当消毒。

## 第 14 周（2026-09-18 起）

| # | 任务 | 状态 |
| --- | --- | --- |
| W14-1 | 装 pytest + ruff | ✅ pytest 9.1.1 / ruff 0.16.8 |
| W14-2 | `UNIT_CASES` 迁到 pytest，探针走 `tmp_path` | ✅ `tests/` + `conftest.py` + `pytest.ini`，迁移 21 条全绿、零 skip |
| W14-3 | 模型评估 3 → 10 条并按四类标注 | ✅ 10 条（四类各 ≥2）+ 判据自检 19 条；**首跑 8/10**（两条 FAIL 为格式漂移，见 T10） |
| W14-4 | 结构重构：`tools.py` 抽 `_iter_notes()`；周脚本抽 `_common.py` | ✅ 见 T11（行为不变的离线验证已过；端到端待你跑 `07_evaluate_agents.py`） |
| W14-5 | `ruff check` + `ruff format` 全量 | ✅ 见 T12：新增 `ruff.toml` 圈定范围，`All checks passed`，format 重排 17 个文件 |
| W14-6 | 交叉验证两条测试入口 | ✅ 见 T13：4 处变异，3 处两条入口一致抓到、1 处都漏已补测试；当前 `pytest` 46 条 / `--unit-only` 12/12 |

### T6 手写 `UNIT_CASES` 迁到 pytest

| 项 | 内容 |
| --- | --- |
| 现象 | 12 条单元用例写死在 `eval_cases.py` 里，靠手写 `finally` 清理探针；`--unit-only` 是唯一的无网入口，加一条用例就要动 600 行的大文件。 |
| 根因 | 测试的**清理责任写在了测试自己身上**：探针建在仓库根，靠 `finally` 删。只要清理逻辑有一处错（T2 就是 `os.rmdir` 删符号链接），失败还会被 `except OSError: pass` 吞掉，于是"绿灯 + 脏仓库"。 |
| 改法 | 新建 `tests/`，`conftest.py` 提供三件装置：`notes_root`（把 `config.NOTES_ROOT` 换到 `tmp_path`）、`write`（在临时根里造笔记）、`make_dir_link` / `make_file_link`（造链接并带沙箱断言）。**清理责任移交给 pytest 的 `tmp_path`**，测试里不再有任何 `finally`。另加 `pytest.ini`（`testpaths` + `pythonpath`）与"导入 config 前先塞占位密钥"（否则没有 `.env` 的环境连 import 都过不去，第 15 周 CI 会直接挂）。 |
| 验证 | `pytest -q` → **21 passed，0 skipped，0.11s**；`eval_cases.py --unit-only` → 12/12；跑完 `find . -type l`（排除 `.venv`）为空、`git status` 无 `_probe_*` 类残留。速度也从 ~5s 降到 0.11s——旧入口每次要扫真实仓库。 |
| 影响面 | 旧入口**暂时保留**作为交叉验证；两套用例并存是评审报告 6.4 的"两套评估风格"问题，处置决定推迟到 W14-6 验收后再定。 |

新旧用例映射（交叉验证的对照表）：

| 旧 | 新 |
| --- | --- |
| U1 | `test_rejects_non_markdown_paths`（4 组参数：`.env` / `../.env` / `/etc/passwd` / `笔记.txt`） |
| U2 | `test_list_excludes_hidden_and_excluded_dirs`（断言更严：精确等于 `["可见.md"]`） |
| U3 / U4 / U5 | `test_rejects_excluded_dir` / `test_rejects_path_outside_root` / `test_rejects_symlink_escape` |
| U6 | `test_link_escape_not_listed_searched_or_read` |
| U7 | `test_search_clamps_results_and_rejects_long_keyword` |
| U8 | `test_bad_types_return_structured_errors`（两个文件各 3 组参数） |
| U9 / U10 | `test_rejects_oversize_file` / `test_clamps_lines_and_reports_truncation` |
| U11 / U12 | `test_check_input_boundaries` / `test_timeout_and_retry_config` |
| （新增） | `test_search_skips_oversize_file`——第 13 周 T1 的固定用例，当时特意留到本步 |

### T7 迁移中我自己踩的坑：fixture 依赖链让探针落回真实仓库

| 项 | 内容 |
| --- | --- |
| 现象 | 首轮迁移后 `test_rejects_symlink_escape` 被 **SKIPPED**，理由写成"当前环境无法创建链接"；而同一台机器上旧的 U5 用例是**通过**的。再跑一次的错误信息暴露真相：`FileExistsError: '/tmp/pytest-of-gold-pps/...' -> '/home/gold-pps/.../agent-learning/逃逸链接.md'`——链接被建到了**真实仓库**里，第一次运行留下的断链让第二次运行直接失败并 skip。 |
| 根因 | `make_file_link` 这个 fixture **没有依赖 `notes_root`**，因此 `monkeypatch.setattr(config, "NOTES_ROOT", tmp_path)` 从未执行，`link = config.NOTES_ROOT / rel_link` 指向真实目录。更阴的是它**只影响一部分用例**：同时请求了 `write`（而 `write` 依赖 `notes_root`）的用例因为 monkeypatch 被别人的依赖链触发而"碰巧"正确。所以症状是"个别用例挂、其它全绿"，与仓库内容无关，只看 fixture 依赖图。 |
| 改法 | ① 两个链接 fixture 改为**显式依赖 `notes_root`**，并从它拼路径（不再读 `config.NOTES_ROOT`）；② 加硬断言 `link.is_relative_to(tmp_path)`，把"探针跑出临时目录"变成**立刻失败**，而不是留个断链慢慢被发现；③ 清掉已经落进仓库的 `agent-learning/逃逸链接.md`。 |
| 验证 | 清理后 `pytest -q` → 21 passed、**0 skipped**（说明链接测试真的在跑，之前的 skip 不是环境问题）；`find . -type l` 除 `.venv` 外为空。 |
| 影响面 | 无代码影响，纯测试装置。但它给本阶段添了一条**普适经验**：`monkeypatch` 系 fixture 的生效靠依赖链触发，**"没报错"不等于"生效了"**；凡是替换全局状态的 fixture，都要配一条断言把假设钉死。 |

### T8 模型评估 3 → 10 条：标四类、断言升级为"引用集合"

| 项 | 内容 |
| --- | --- |
| 现象 | 模型评估只有 3 条，判据是"文件名作为子串出现在回答里"＋单个关键词。只要回答里顺嘴提过文件名就算"有引用"，无法区分"真的给出可回查的出处"和"提到了一下"。用例也没有分类，看不出模型在哪类问题上弱。 |
| 根因 | ① 断言口径太弱，与第一阶段定的引用格式（`[文件名:起始行-结束行]`）不匹配——格式约定写在指令里，判据却只做子串匹配；② 用例不分类，第 19 周的检索评测集（四类各 ≥5）没有可继承的架子；③ 缺少"测量工具自检"，断言写错时分数看起来依然合理。 |
| 改法 | ① 10 条用例，四类各 ≥2：**有答案 4 / 无答案 2 / 跨文档 2 / 需要多跳 2**；② 新增 `extract_cited_files()` 解析 `[文件名:行号]`，判据升级为 `must_cite_files`（都要出现）、`must_cite_any_of`、`min_distinct_files`（跨文档主判据）、`forbid_citations`（无答案类禁止编造出处）、`must_contain_any`（拒答措辞不唯一）；③ 输出按类别汇总，新增 `--only A1` 单条调试；④ 新增 `tests/test_eval_cases.py`（17 条离线自检），把"测量工具"本身纳入回归。 |
| 验证 | `pytest -q` → **38 passed**（21 → 38，新增的全是判据自检，不联网）；`--unit-only` → 12/12 不变；`--only` 过滤生效。**10 条模型用例的真实跑分待用户在低谷时段执行**（预计 0.3~0.5 元）。 |
| 影响面 | 旧 M1–M3 被替换（题面与判据都变了，历史分数不可直接比较——第 19 周做对比表时要注意这点）。`eval_cases.py` 的单元部分保持不变，仍作交叉验证入口。 |

**前提：每道题的答案先在资料里核实过。** 10 条全部用 `grep` 确认过 ground truth（例如"20.7878"确在 `第7周笔记.md`；"junction"在第 11 周笔记出现 6 次），否则一道本身无解的题会被读成"模型能力差"——这是评测集最贵的错误。

**发现一：评测集的题面会被资料库自己污染，而且这个病会复发。**

- 旧 M3 问的是"用 Excel 怎么画图"，而"Excel 画图"这几个字后来被写进了第 9/10/11/12 周笔记（作为评估用例本身被记录）——检索型 Agent 很可能检索到那几个字，给出"笔记里提到过"这种半对半错的回答。
- **本轮我原样又犯了一次**：我把两条"无答案"负样本的关键词、以及"已核实资料里 0 命中"这句结论写进了本文件。本文件位于 `agent-learning/` 下、**属于可检索范围**，于是其中一条用例的回答直接引用了这句话作为"资料里确实没有"的依据——**等于把答案卡发下去了**。那一轮的 PASS/FAIL 因此不可信。

**据此定下的规则**：

1. 无答案题的**关键词与答案卡只写在 `notes-qa/` 内**——`notes-qa` 已在 `tools.EXCLUDE_DIRS` 里，Agent 检索不到；第 19 周的 `eval/` 按目录约定也在这里，天然安全；
2. 资料库里可以写"我们怎么设计评测"（方法论），但**不能写出负样本关键词本身**；本条目已按此改写，两条负样本关键词已更换为资料库里 0 命中的新词，且不再落在本文件中；
3. 第 19 周扩到 20 条时，加一步自动检查：**负样本关键词是否已出现在 `agent-learning/` 的任一 `.md` 里**（一条 grep 就能做）。

**发现二：我自己写的判据自检一开始就是错的。** 第一条测试同时给了 `must_contain` 和 `must_contain_any`，构造的答案满足不了后者，于是测试报错——而**被测代码是对的**。教训：**判据自检要一条测试只验一件事**，否则失败时分不清是工具坏了还是测试写错了。拆成两条后 38 passed。

### T9 我自己的一次破坏性操作：`git checkout --` 冲掉了同文件的其它改动

| 项 | 内容 |
| --- | --- |
| 现象 | 为验证"负样本关键词泄漏检查"确实会报警，我往 `学习进度.md` 末尾追加了一行关键词，跑完检查后用 `git checkout -- agent-learning/学习进度.md` 还原。结果该文件**所有未提交改动**（第 13 周的日期、相关文档、当前位置、第 13 周小节四处）一起被冲掉，`git status` 里它从"已修改"直接变成干净——**注意这个信号本身很隐蔽：丢改动不会报错，只会"看起来干净了"**。 |
| 根因 | `git checkout -- <path>` 的语义是"把该文件恢复到 HEAD 版本"，不是"撤销我刚才那一行"。它**不区分**"我这次的临时改动"和"之前积累的未提交改动"。这与仓库规则"不要在用户未要求时执行 `git commit` / `git push`"是同一类问题的另一面：git 的还原类命令都是**文件级**的，没有局部撤销。 |
| 改法 | ① 立即用 `apply_patch` 把四处改动补回（本轮对话里有原始内容，可完整重建）；② 定下做法：**清临时改动一律用 `apply_patch` 精确删行，不用 `git checkout --` / `git restore`**；③ 需要"就地制造污染"的验证，改成写临时文件或 `tmp_path`。 |
| 验证 | 恢复后 `grep` 确认四处改动俱在、`学习进度.md` 重新出现在 `git status` 里，`pytest` 40 条仍全绿。**反向验证**：泄漏检查本身工作正常——把关键词写进资料库后该测试立刻 FAIL，还原后 PASS。 |
| 影响面 | 本次无内容丢失（改动可重建）。但这是一次真实的数据损失风险：如果被冲掉的是只存一份的笔记正文，就补不回来了。**教训："我打算怎么撤销它"应当在制造污染之前就想清楚，而不是之后。** |

### T10 模型评估首跑（10 条，2026-09-18）：8/10，两条 FAIL 都是引用格式漂移

| 项 | 内容 |
| --- | --- |
| 现象 | 全量跑 20/22（12 条单元 + 8/10 模型）。C1（跨文档）与 D2（多跳）FAIL，判据报"引用文件数 0 < 2：[]"，看起来像"根本没给出处"。 |
| 真相 | 看回答节选：模型**引用了正确的文件与行号**，只是写成了反引号代码块 `` `学习Agent规划.md:133-141` ``、`` `工程化改造记录.md:158-166` ``，而不是指令约定的 `[文件名:起始行-结束行]`。`extract_cited_files()` 只认方括号格式，于是整段引用被判成"无"。 |
| 根因 | 两层：① **判据把"引用内容对不对"和"引用格式对不对"混在一条断言里**，格式不合规被记成"没找到出处"，会直接误导能力判断；② 模型在长回答里会漂移出约定格式（C1 输出 4,141 token，是本轮最长的一条），而这是**真实的遵从性问题**——第 23 周 Web 界面要按这个格式解析来源、G2 的"引用准确率"也依赖它。 |
| 处置（下次开工第一件事） | ① `extract_cited_files()` 增加对反引号格式的解析（用于判断引用**内容**是否正确）；② 另加一条独立断言（如 `require_bracket_citation`）单独统计**格式合规率**——能力指标与遵从性指标必须分开，否则永远分不清"找不到出处"和"不守格式"；③ 让 `eval_cases.py` 保存**完整回答与引用列表**（现在只打印 300 字节选，FAIL 之后无从复查，这轮就是靠人肉读节选才看出真相）。 |
| 验证 | 待实施后重跑 10 条。预期：C1/D2 在"引用内容"维度转为通过，在"格式合规"维度如实记 FAIL。 |
| 影响面 | **本轮分数不能直接当能力基线**：跨文档 1/2、多跳 1/2 这两个数字里混着格式问题。第 19 周做量化对比表之前，必须先跑通修正后的判据再记录基线。 |

本轮逐条用量（请求数 = 该题里"思考 + 工具往返"的次数，能解释成本差异）：

| 用例 | 类别 | 结果 | 请求 | 输入 | 输出 |
| --- | --- | --- | --- | --- | --- |
| A1 第 7 周做了哪几件事 | 有答案 | PASS | 6 | 22,146 | 1,213 |
| A2 两个护栏的运行时机 | 有答案 | PASS | 3 | 3,971 | 529 |
| A3 路径安全的做法 | 有答案 | PASS | 6 | 20,732 | 2,427 |
| A4 账单金额 | 有答案 | PASS | 3 | 4,748 | 1,025 |
| B1 无答案（容器编排部署） | 无答案 | PASS | 5 | 10,910 | 1,097 |
| B2 无答案（桌面界面框架） | 无答案 | PASS | 4 | 8,474 | 775 |
| C1 哪些周做了安全工作 | 跨文档 | FAIL（格式） | 6 | 40,639 | 4,141 |
| C2 notes-qa 第 9→12 周的变化 | 跨文档 | PASS | 4 | 23,637 | 2,332 |
| D1 演示稿漏洞追到出处 | 需要多跳 | PASS | 6 | 35,067 | 3,991 |
| D2 规划要求追到落地记录 | 需要多跳 | FAIL（格式） | 6 | 27,604 | 1,879 |
| **合计** | | **8/10** | 49 | **197,928** | **19,409** |

值得记住的两条：

1. **成本与"工具往返次数"强相关**，不是与问题长短：最贵的 C1（40,639 输入）跑了 6 次请求，
   最便宜的 A2 只跑 3 次——单条成本差 10 倍。这与第 13 周 T3 的实测结论完全一致。
2. **无答案类 2/2 通过**：修正拒答判据（改用正则、去掉"禁止引用"）之后，拒答行为是可靠的。
   本轮另按低谷价粗估总花费约 0.15 元（高峰约 0.3 元；未记录缓存命中量，属可改进项）。

### T11 W14-4 结构重构：过滤规则与接入配置各收敛成一份

| 项 | 内容 |
| --- | --- |
| 现象 | ① `tools.py` 里"排除目录 + 隐藏文件 + 链接逃逸"三条过滤规则被 `list_notes` 与 `search_notes` 各抄一份；② 8 个周脚本各自抄了一遍 DeepSeek 接入样板（读 `.env` → 取密钥 → 建客户端 → 三步设置），连"没有找到 DEEPSEEK_API_KEY"的提示语都一样；③ 模型名 `"deepseek-flash"` 硬编码 12 处。 |
| 根因 | 抄写的成本不对称：**复制的时候最便宜，修改的时候最贵**。第 10 周修 `read_note` 的排除目录漏洞时，`search_notes` 就得单独再修一次——安全规则抄两份等于"改一处、漏一处"，而漏的那处不会有任何报错。 |
| 改法 | ① `tools.py` 新增 `_iter_notes()` 生成器，三类过滤只此一份，产出 `(相对路径, 绝对路径)`；② 新增 `agent-learning/_common.py`（`MODEL` / `client` / `sync_client` + 三步设置），8 个脚本改为一行 import；③ 脚本里统一 `model=MODEL`；④ 顺手把 `except Exception` 收窄为 `except (OSError, UnicodeDecodeError)`。 |
| 验证 | `pytest -q` 42 条全绿；`--unit-only` 12/12；**真实仓库行为不变**（重构前后都是 list 20 篇 / search 5 条）；10 个脚本 AST 通过；`_common` 可独立加载（MODEL 与 base_url 正确）。端到端验证留给你：`python 07_evaluate_agents.py` 会依次运行 02/03/04 三个脚本，正好充当这次重构的回归测试。 |
| 影响面 | 批量机械重写用了脚本 + 每步断言（第一次运行就抓到一次**假阳性**：断言把文档串里的 `DEEPSEEK_API_KEY` 误当成裸 `API_KEY` 残留）；`_common.py` 与 `notes-qa/config.py` **有意不合并**——后者额外带超时与重试、且属项目代码，跨目录共享会让项目依赖练习脚本。 |

### T12 W14-5 ruff 全量：范围先圈定，再谈"全绿"

| 项 | 内容 |
| --- | --- |
| 现象 | `ruff check .` 报 **51** 个问题，散落在 `agent-learning/`、`python-base/` 和仓库根。 |
| 根因 | ① 没有任何 ruff 配置 → 检查范围是"整个仓库"，把 `python-base/`（`AGENTS.md` 写明"暂未整理"）和仓库根那堆 PPT 工具（`_build.py`、`_export.py`、`_shot.mjs`、`output/`，由另一台机器合并进来）全算了进来；② `agent-learning/` 内的 33 个里，绝大多数是**冗余 `# noqa: F401` 与导入未排序**，真正的代码问题只有 5 个。**"51 个错误"和"33 个错误"都不是坏消息，前提是先说清范围。** |
| 改法 | ① 新建 `ruff.toml`：`include = ["agent-learning/**/*.py"]` 圈定学习代码，排除项连同理由写在文件里；规则集用**默认**而不 `select = ["ALL"]`（否则会收到几百条风格意见，结局通常是把配置整个关掉）；② `ruff check --fix` 修掉 28 项；③ 剩下 5 项逐个人工判断：3 处 catch-all 是**有意为之**（命令行会话与评估 harness 不能被单条异常终结）→ 加 `# noqa: BLE001` 并把理由写在上一行；1 处 `subprocess.run` 缺 `check` → 显式写 `check=False`；1 处 async 函数里阻塞写文件 → 改 `asyncio.to_thread`；④ `ruff format` 重排 17 个文件。 |
| 验证 | `ruff check .` → **All checks passed**；format 之后再 check 仍通过；`pytest -q` 42 条全绿（格式化未破坏任何行为）。 |
| 影响面 | ① **`--fix` 会删掉"多余"的 noqa**：我先前写的 `# noqa: BLE001 —— 说明文字` 被它当成无效指令删掉了，教训是 noqa 必须写规范（`# noqa: BLE001` 收尾，说明另起一行）；② `ruff format` 改了 17 个文件的排版，是本阶段最大的一笔机械改动；③ 仓库根新出现的 PPT 工具与产物、`python-base/`、以及一个 0 字节的 `_wm_extract.py`（与第 12 周删掉的空 `agent.py` 同类），都属第 15 周"仓库瘦身"的对象——本轮**只在 ruff 范围里排除，没有删除**。 |

### T13 W14-6 交叉验证：用变异测试验证"两条入口结论一致"

| 项 | 内容 |
| --- | --- |
| 现象 | 任务要求"`pytest -q` 与 `eval_cases.py --unit-only` 结论一致"，但"一致"不能靠肉眼比对映射表（T6）就算完。 |
| 根因 | 迁移最容易出的错是**漏用例**：某条旧用例管的行为，在新套件里没人管——而两条入口都还是绿的，从结果上看不出来。 |
| 改法 | 把 `notes-qa/` 复制到临时目录，**故意改坏一个行为**（变异），再分别跑两条入口，看是否都报警。共 4 处变异，都只动临时副本（这条纪律来自 T9 的教训）。 |
| 验证 | 见下表：3 处两条入口都抓到；1 处两条都漏，已补测试。 |
| 影响面 | 第 4 行暴露的缺口已补：新增 `tests/test_documented_limits.py` 钉住 4 个**文档承诺过的**数字（1MB / 200 行 / 5 条 / 500 字符），并确立原则——**只钉文档承诺值，不钉明确待调的初值**（如 T3 的 `MAX_HISTORY_TURNS`）。 |

| 变异 | 新入口（pytest） | 旧入口（--unit-only） | 结论 |
| --- | --- | --- | --- |
| `read_note` 大小检查放宽 100 倍 | 1 failed | 11/12 | 一致 ✅ |
| 去掉 `_safe_resolve` 的越界拦截 | 3 failed | 9/12 | 一致 ✅ |
| 去掉"排除目录 + 隐藏文件"过滤 | 1 failed | 11/12 | 一致 ✅ |
| `MAX_READ_LINES` 200 → 199 | 46 passed（全绿，补测试前 42 passed） | 12/12 | **两条都漏** ⚠️ → 已补 |

为什么第 4 行两条都漏：两个套件的边界用例都写成 `assert end_line == tools.MAX_READ_LINES`，
也就是**拿常量跟自己比**。这在"调参"时是优点（改常量不会误报），在"常量被误改"时是盲区。
补法不是把行为测试改成硬编码（那会让调参误报），而是**另加一条只盯文档承诺值的测试**。

### T14 第 14 周验收（2026-09-22）

对照 `第二阶段学习规划.md` 第 14 周的验收标准（`pytest -q` 全绿、`ruff check` 无错、两条测试入口一致）：

| 检查项 | 结果 |
| --- | --- |
| `ruff check .` | **All checks passed**（配置见 T12） |
| `pytest -q` | **46 passed**（迁移 21 + 判据自检 21 + 文档承诺值 4） |
| `python eval_cases.py --unit-only` | 12/12 |
| 两条入口一致性 | 变异测试 4 处：3 处两条都报警、1 处都漏已补测试（见 T13） |
| 模型评估 10 条（四类） | **10/10**：有答案 4/4、无答案 2/2、跨文档 2/2、需要多跳 2/2 |
| 引用格式合规率 | **8/8**（新增的独立指标，见 T10） |
| 周脚本重构端到端 | `07_evaluate_agents.py` → **3/3**（依次跑 02/03/04） |
| 完整回答留档 | `eval_last_run.json`（`--save` 生成，已 gitignore） |

**成本与一个必须记住的观察**：本轮 10 条评估 367,904 输入 + 24,821 输出 token、67 次请求，
低谷价粗估约 0.25~0.3 元。对比 9-18 那次首跑：**通过率 8/10 → 10/10，但 token 从 21.7 万涨到 39.3 万**
（请求数 49 → 67，最贵的 D2 一条 12 次请求 / 10.6 万输入 token）。这说明
**"更准"可能只是"更贵"，通过率与成本必须同时记录**——第 19 周做四组对比表时尤其要注意。

**记账口径漏洞**：`07_evaluate_agents.py` 用 subprocess 跑其它脚本，**没有任何 token 统计**，
所以那 3 条用例这次的成本没记上。属于待补的口径，不是失败项。

## 滚动待办（截至 2026-09-22）

| # | 事项 | 状态 |
| --- | --- | --- |
| — | ~~重跑 10 条模型评估拿可信基线~~ | ✅ 2026-09-22：22/22，四类全满，格式合规 8/8（见 T14） |
| — | ~~端到端验证周脚本重构（`07_evaluate_agents.py`）~~ | ✅ 2026-09-22：3/3 |
| — | ~~修评估判据的"格式 vs 内容"混装~~ | ✅ 已完成（`extract_cited_files` 认两种写法 + `require_bracket_citation` 独立统计 + `--save` 存全文） |
| 🟡 | 给 `07_evaluate_agents.py` 加 token 用量统计 | 记账口径缺口（见 T14） |
| 🟢 | 用 15~20 轮重跑 T3 曲线（第 13 周遗留） | 8 轮用不满窗口，测不出收敛效果 |
| 🔴 | 仓库瘦身：4 个 PPT + 根目录 PPT 工具与产物 + `python-base/` + 0 字节 `_wm_extract.py` | 第 15 周主任务（本轮只在 ruff 范围排除） |
| — | ~~补 T3 的真实 token 曲线~~ | ✅ 已完成（两臂各 8 轮，结果见 T3 与第 13 周笔记第 3.4 节） |
| — | 用 15~20 轮重跑 T3 曲线，并验证"截断是否降低缓存命中率" | 待做（第 14 周可选） |
| — | 重启 VS Code / 重新登录后确认 `environment.d` 生效 | 待你确认 |
| — | ~~轮换 DeepSeek API Key~~ | 已决策：不轮换（见 T5 决策记录） |

## 跨阶段的方法论备注

1. **改前先能复现**：T2 的价值一半来自那条最小复现命令——它把"某天测试跑完多了个文件"变成了 5 行、可在任何机器上重放的事实。
2. **注意"绿灯下的脏数据"**：测试通过 ≠ 系统干净。清理逻辑要有独立的验证手段（本例是 `ls`/`git status`），不能靠测试结论倒推。
3. **同一个常量、两种策略**：`MAX_FILE_BYTES` 在 `read_note` 是"报错"，在 `search_notes` 是"跳过 + 计数"。别为了"统一"把两种合理语义强行合并。
4. **别猜第三方库的数据结构**：T3 的截断逻辑整个建立在"`to_input_list()` 返回 dict、工具返回塞在 `output` 字符串里"这个前提上。这个前提是打开 `.venv/.../agents/items.py` 读出来的，不是猜的——猜错的后果很隐蔽：代码照常运行，一个字符也没截到，而且没有任何报错。**函数签名同样算"数据结构"**：写实测脚本时我猜 `Runner.run(..., model_settings=...)`，实际是 `run_config=RunConfig(model_settings=...)`。数据猜错是静默失效，参数名猜错是当场 `TypeError`——后者其实更幸运。查签名的成本是 10 秒，返工的成本是一轮往返加一次被用户撞见的报错。
5. **"清理文件"和"密钥泄露"是两种性质，用 git 历史来区分**：删任何带密钥的文件之前，先 `git log --all --name-only` 查它进没进过提交。没进过 = 本地卫生问题，删掉即可；进过且 push 过 = 已经泄露，必须轮换密钥，此时"删掉文件"只是自欺欺人。T4a 的结论是前者，所以可以放心删。
6. **改配置先备份，并先想好回滚命令**：T4b 是本周唯一会让人"打不开工具"的改动。备份特意放在 `/tmp`（重启即清空的临时位置），避免为了回滚反而多留一份长期密钥副本；台账里直接写可执行的回滚命令，而不是"如有问题请恢复备份"。
7. **同一个秘密别到处抄**：为了覆盖终端、GUI、脚本三种启动路径，很容易在 `.bashrc`、`.profile`、`environment.d` 里各抄一份密钥。原则是**密钥本体只存一份（600 权限），其他位置只放"引入"这一行**。唯一的例外是 `environment.d`——它不是 shell，没有 source 机制，只能内联值；遇到这种必须重复的情况，要在台账里写清"哪一份是真副本、为什么必须重复"，否则半年后没人分得清哪个能删。
8. **贴输出之前先问一句"它会落到哪"**：验证类命令的输出常常自带敏感值（`env`、`systemctl --user show-environment`、`printenv`、带凭据的 URL）。终端里看一眼没问题，**但一旦贴进对话，它就同时进了会话记录、本地 rollout/sqlite，并会随上下文发给模型服务**。自检习惯：先把输出过一遍 `grep -v` 或做成"只看键名/只看长度"，再决定贴什么。这一条要是当初就执行了，T5 整节都不必存在。
9. **别用 git 的还原命令清"自己的临时改动"**：`git checkout -- <path>` / `git restore` 是**文件级**的，会把该文件所有未提交改动一起抹掉，而且**不会报错**（T9 的现场：文件从"已修改"变成干净，看起来像是"清理好了"）。清临时行用 `apply_patch`；要造污染做验证，就写临时文件或 `tmp_path`，别碰真实文档。
