# 学习进度

> 最后更新：2026-09-18（新增第二阶段第 13~14 周进度与"交接单"）
> 用途：在另一台电脑继续学习时，先读本文件恢复上下文。
> 路径说明：Agent 学习目录为 `agent-learning/`，基础练习目录为 `python-base/`。
> 相关文档：`学习Agent规划.md`（第一阶段 12 周计划）、`第二阶段学习规划.md`（第 13~24 周）、`工程化改造记录.md`（技术债台账）、`笔记索引.md`（笔记与产出对照）、`学习评审报告.md`（2026-09-15 评审结论与处理状态）。

## 当前位置

学习 Agent 开发，后端使用 **DeepSeek API**，开发框架用 **OpenAI Agents SDK**。所有请求都发往 `https://api.deepseek.com`，不需要 OpenAI 充值。

当前进度：**第一阶段 12 周全部完成；第二阶段进行中——第 13 周已完成，第 14 周完成 2/5**。
第二阶段目标与验收标准见 `第二阶段学习规划.md`，技术债逐条记录见 `工程化改造记录.md`。

## 交接单（给下一位 AI / 恢复上下文的自己）

> 本文件是恢复上下文的入口。下面按顺序读，十分钟能接上全部上下文。

### 读什么

1. **本文件**：当前位置 + 下一步，看到这里就够开工；
2. `第二阶段学习规划.md`：第 13~24 周的计划与**验收标准**。**周计划的勾选已按实际回填**，
   它是"完成 / 未完成"的唯一真相来源；第一节的四组验收清单（G1~G4）按设计留到各自的验收周
   再逐条打勾（G1 在第 15 周末），**现在还没打勾不等于没做**；
3. `工程化改造记录.md`：技术债台账，每条都是"现象 → 根因 → 改法 → 验证"。
   **T10 的第一个处置项就是下次开工第一件事**；T3 里还有一条"参数待用 15~20 轮重测"；
4. `第13周笔记.md`：第 13 周复盘（含 T3 真实 token 曲线的反转结论，别照着旧结论讲）。

### 现在在哪

第 14 周进行中：`UNIT_CASES` 已迁到 pytest（`tests/` 共 **40 条**离线测试全绿）；
模型评估已扩到 10 条并按四类标注，**首跑 8/10**——两条 FAIL 是引用格式漂移、不是找不到出处
（详见台账 T10）。剩余：结构重构、ruff 全量、交叉验证。

### 下一步（按顺序，第 1 件最重要）

1. **修评估判据的"格式 vs 内容"混装**（台账 T10）：`extract_cited_files()` 同时认
   `` `文件.md:行号` `` 与 `[文件:行号]`；另加 `require_bracket_citation` 单独统计**格式合规率**；
   让 `eval_cases.py` 保存完整回答（现在只有 300 字节选，FAIL 后无从复查）。
   改完重跑 `python eval_cases.py`（用户时段、低谷价），那才是可信的能力基线；
2. 结构重构（W14-4）：`tools.py` 抽 `_iter_notes()` 统一过滤规则；周脚本 00~07 抽 `_common.py`；
3. `ruff check --fix .` + `ruff format .`（W14-5；现状 19 个问题），跑完确认 `pytest -q` 仍全绿；
4. 交叉验证（W14-6）：对照台账 T6 的映射表，确认 `pytest -q` 与
   `python eval_cases.py --unit-only` 结论一致；
5. 收尾：写 `第14周笔记.md`，并把本文件与规划文件的第 14 周状态改成"完成"。

### 动手前先知道这五条（都是本阶段真踩过的）

1. **模型评估由用户在自己时段跑**（会花 token）；AI 侧只跑 `--unit-only` 与离线测试。
   10 条一轮约 20 万 token，低谷价约 0.15 元；
2. **重活放低谷**：工作日 12:00–14:00、18:00–次日 09:00、周末全天，价格是高峰的一半；
3. **不要用 `git checkout --` / `git restore` 清临时改动**——它是文件级的，会把该文件
   **所有**未提交改动一起冲掉，而且不报错（台账 T9 的现场）；
4. **无答案题的关键词不能写进 `agent-learning/*.md`**——那等于把答案卡交给被检索的 Agent
   （台账 T8"发现一"）。题库与答案卡只写在 `notes-qa/` 内（该目录已在 `EXCLUDE_DIRS` 中）；
5. 未经用户明确要求**不要 `git commit` / `git push`**（仓库规则）。

## 已完成

### 第 1 周：概念与环境

- 已注册 DeepSeek 平台并配置 API Key（存放在 `agent-learning/.env`，已被 `.gitignore` 忽略，**不要提交到 GitHub**）。
- 本机 Python 环境位于仓库根目录 `.venv`（**Python 3.14.4**；代码最低要求 ≥3.12），已安装：
  - `openai`
  - `openai-agents`
  - `python-dotenv`

### 第 2 周：跑通第一个 Agent

- [x] `agent-learning/00_first_api_call.py`：普通聊天调用跑通，能正常返回 DeepSeek 回答。
- [x] `agent-learning/01_hello_agent.py`：最小 Agent 跑通，正确回答“罗马帝国何时灭亡”。
- [x] 理解关键概念：`AsyncOpenAI(base_url=...)`、`set_default_openai_api("chat_completions")`、`Agent`、`Runner.run`、`final_output`。

接入 DeepSeek 的关键配置（三个脚本通用）：

```python
client = AsyncOpenAI(api_key=..., base_url="https://api.deepseek.com")
set_default_openai_client(client=client, use_for_tracing=False)
set_default_openai_api("chat_completions")
set_tracing_disabled(disabled=True)
```

### 第 3 周：让 Agent 学会使用工具

- [x] `agent-learning/02_agent_with_tool.py` 已跑通工具调用循环。
- [x] 已注册 3 个 `@function_tool` 工具：
  - `get_beijing_time()`：返回当前北京时间；
  - `count_students_rows()`：统计 `students.csv` 数据行数（不含表头），当前文件为 11 行；
  - `find_student_home(name)`：按姓名在 `students.csv` 中查找家乡，带参数工具示例。
- [x] 观察到“模型决定调用工具 → Python 执行 → 结果返回模型 → 组织回答”的完整循环。
- [x] 理解工具说明书的 JSON Schema 长什么样（参数名、类型、required、additionalProperties）。
- [x] 使用 Chat Completions 兼容路径，未使用 Responses 专属工具能力。

已验证示例输出：

```text
[工具被调用] find_student_home(name=cain)
根据查询结果，Cain 的家乡是 Bible。
```

### 第 4 周：理解运行循环与状态

- [x] 阅读 OpenAI Agents SDK 的 Running agents 文档。
- [x] 掌握多轮对话如何保留上下文（history / session / continuation）。
- [x] 不使用 OpenAI 专属的服务端 continuation，改为手动传 `history`。
- [x] 做小练习：连续提问，Agent 能记住之前的信息。

产出：

- `agent-learning/03_multiturn_memory.py`：手动传 history 的多轮记忆 Demo（已跑通）。
- `agent-learning/03_no_history_test.py`：不传 history 的对照实验（模型答不出）。
- `agent-learning/第4周笔记.md`：三种续接方式区别与实验记录。

### 第 5 周：多 Agent 编排与交接

- [x] 阅读 OpenAI Agents SDK 的 Orchestration and handoffs 文档。
- [x] 创建总控 Agent + 两个专长 Agent（历史老师、数学老师）。
- [x] 总控能根据问题类型自动交接，`result.last_agent` 可确认实际回答者。
- [x] 了解 Agent as tool 与 handoffs 的适用区别。

产出：

- `agent-learning/04_multi_agent_handoff.py`：总控分流 + handoffs Demo（已跑通）。
- `agent-learning/第5周笔记.md`：交接机制、踩坑记录与对照总结。

### 第 6 周：安全与护栏

- [x] 阅读 OpenAI Agents SDK 的 Guardrails and human review 官方文档。
- [x] `agent-learning/05_guardrails.py` 已创建并验证：
  - 输入护栏 `block_dangerous_operations` 能拦截危险操作请求；
  - 输出护栏 `block_secret_leak` 能拦截疑似 API Key 泄露。
- [x] `agent-learning/06_human_approval.py` 已创建并验证：
  - 敏感工具 `delete_file` 使用 `needs_approval=True`；
  - `--decision reject` 和 `--decision approve` 两条路径均已跑通。
- [x] 已运行两个脚本并填写 `agent-learning/第6周笔记.md` 的“我的复盘”。

产出：

- `agent-learning/05_guardrails.py`：输入护栏 + 输出护栏 Demo；
- `agent-learning/06_human_approval.py`：敏感操作审批 Demo；
- `agent-learning/第6周笔记.md`：安全设计与实验记录（复盘已填写）。

## 第 7 周：追踪与评估（已完成）

任务清单（对应 `学习Agent规划.md`）：

- [x] 保持代码里 `set_tracing_disabled(disabled=True)`，不在 OpenAI 平台开追踪。
- [x] 需要调试时在本地打印关键日志：模型回复、工具调用参数、工具返回结果、交接目标。
- [x] 熟悉 DeepSeek 平台的用量/账单页面，每次实验后查看花费。
- [x] 阅读 OpenAI Agents SDK 的 Evaluate agent workflows 文档（评估脚本本地自己写）。
- [x] 为前几周的 Agent 写 2～3 条评估用例。

产出：

- `agent-learning/07_evaluate_agents.py`：3 条本地评估用例。
- `agent-learning/第7周笔记.md`：日志、评估、token 用量和账单记录。
- 账单记录：2026-09-01～09-10 合计 20.7878 元。

## 第 8 周：项目启动（已完成）

选定项目：**个人资料问答助手**（读取本地 Markdown 笔记回答问题）。

第 8 周任务：

- [x] 确定项目范围与 MVP；
- [x] 画出功能结构（Agent + list/search/read 工具）；
- [x] 和 Codex 确认设计后可开始第 9 周编码。

设计文件：`agent-learning/notes-qa/项目设计.md`

## 第 9 周：项目 MVP（已完成）

产出目录：`agent-learning/notes-qa/`

- [x] `config.py`：DeepSeek + SDK 配置，统一 `MODEL = "deepseek-flash"`；
- [x] `tools.py`：list_notes / search_notes / read_note，含路径安全与大小限制；
- [x] `agent.py`：三个 `@function_tool`，指令含引用格式与"不编造"要求；
- [x] `main.py`：单次命令行问答，stderr 打印 token 用量；
- [x] `eval_cases.py`：2 条单元测试 + 3 条模型评估，**5/5 通过**（第 9 周当时的数字；第 10、11 周陆续扩充，现为 12 单元 + 3 模型 = 15 条）；
- [x] 跑通"提问 → 搜索 → 读取 → 回答 + 引用"。

关键行为已验证：

- 路径穿越被拒（`../.env`、`/etc/passwd`、`../学习进度.md`）；
- `list_notes` 排除 `.env` 与 `notes-qa/`；
- 无答案问题明确回答"笔记中没有找到"，不编造；
- 跨文件引用生效（一次回答可引用多篇笔记）。

第 9 周笔记：`agent-learning/第9周笔记.md`

## 第 10 周：项目收尾与交互升级（已完成）

- [x] 修 `read_note` 排除目录漏洞（`notes-qa/` 不再可读）；
- [x] `search_notes` 加每文件上限 2 条，避免总览文档垄断结果；
- [x] `main.py` 加 `check_input`：超 500 字符直接拒绝，不走模型；
- [x] `main.py` 升级为交互式循环，支持多轮历史、`/new`、`/exit`；
- [x] 边界输入测试：空输入、空格、500 个 a、emoji；
- [x] 评估用例扩到 8 条（5 单元 + 3 模型），**8/8 通过**（第 10 周当时的数字；第 11 周扩到 15 条）；
- [x] 设计文档追加 `## 13. 第 10 周更新`。

关键行为已验证：

- 多轮历史生效：第 2 问只花 1 个请求（不重新搜索，直接用历史）；
- `/new` 清空历史后同样问题请求数从 1 恢复到 4；
- 500 个 `a` 被 `check_input` 拦下，不走模型；
- 无答案问题（Excel 画图）明确回答"笔记中没有找到"，不编造。

第 10 周笔记：`agent-learning/第10周笔记.md`

## 第 11 周：产品线进阶（已完成）

Codex 侧练习，不是 Python 代码。

- [x] 写 `AGENTS.md`（仓库根），验证 Codex 每次会话自动加载；
- [x] 建第一个自定义 Skill：`.codex/skills/notes-qa-eval/SKILL.md`；
- [x] `eval_cases.py` 加 `--unit-only` 参数，支持无网络降级；
- [x] 接入 `filesystem` MCP（写在 `~/.codex/config.toml`，边界锁到仓库根）；
- [x] Subagents 尝试——子代理能启动、能执行，但收不到任务载荷，已详细记录。

副产品：让 Codex 审查项目，发现并修复多个问题：

- `read_note` 非整数行号抛异常（真 bug）；
- `search_notes` 的 junction 逃逸漏洞（真漏洞，会搜到根目录外）；
- `read_note` 的 `truncated` 语义错误（真 bug）；
- `eval_cases.py` 的 `must_cite` 未归一化、M3 期望词太弱；
- `config.py` 加超时与重试配置。

评估结果：单元 12/12 + 模型 3/3 = **15/15 通过**。

第 11 周笔记：`agent-learning/第11周笔记.md`

## 第 12 周：复盘与展示（已完成）

不写新代码，整理成果。

- [x] 仓库瘦身：删掉 5 个残留文件（`projects/`、`test/`、空 `agent.py`、失效 `test.py`）；
- [x] `.gitignore` 补 `.mypy_cache/`、`.pytest_cache/`；
- [x] 写 `README.md`（仓库门面，给人看）；
- [x] 写 `agent-learning/笔记索引.md`（每周主题 / 笔记 / 产出对照）；
- [x] 写 `agent-learning/第12周演示稿.md`（10 分钟汇报大纲）；
- [x] 写第 12 周笔记，含"下一步想做的方向"。

### 12 周总结

| 指标         | 数值               |
| ------------ | ------------------ |
| 周练习脚本   | 9 个（00~07，其中 03 含两份） |
| 周笔记       | 12 篇（第 1~12 周各一篇） |
| 项目代码文件 | 6 个               |
| 单元测试     | 12 条              |
| 模型评估     | 3 条               |
| 评估通过率   | 15/15              |
| 学习总花费   | 约几十元人民币     |

第 12 周笔记：`agent-learning/第12周笔记.md`

### 学习计划完成

12 周学习计划全部完成。后续可选的下一步方向见第 12 周笔记第 5 节。

## 第二阶段：第 13 周（已完成，2026-09-18）

主题：技术债清偿（上）。本周不写新功能，把"能跑"的代码修到"可信"。逐条记录见
`工程化改造记录.md`，完整笔记见 `第13周笔记.md`。

- [x] T1 `search_notes` 加 `MAX_FILE_BYTES` 检查（放在 `read_text()` 之前，超限跳过并回传 `skipped_large_files`）；
- [x] T2 修 U6 清理 bug：`os.rmdir(link)` → `os.unlink(link)`；跑完 `--unit-only` 不再残留 `_probe_*`；
- [x] T3 `main.py` 会话历史收敛（`MAX_HISTORY_TURNS=8` + `MAX_HISTORY_ITEM_CHARS=2000`，只做在写入侧）；
- [x] T4a 清理残留 `agent/` 目录（先确认未被跟踪、内容是副本、全历史未提交过 `.env`）；`.gitignore` 去掉两条历史路径；
- [x] T4b `~/.codex/config.toml` 明文 Key → `env_key = "DEEPSEEK_API_KEY"`，密钥落在
      `~/.config/deepseek/api.env` 与 `~/.config/environment.d/deepseek.conf`（均 600）；
- [x] T5 密钥泄露面盘点（把验证输出贴进对话导致 Key 外泄，结论：见台账 T5 决策记录）；
- [x] 新建 `工程化改造记录.md`（台账）并写入 T1~T5。

验收：`python eval_cases.py --unit-only` **12/12**，`git status` 无 `_probe_*` 残留。

T3 的真实 token 曲线实测（两臂各 8 轮）推翻了"收敛能省钱"的预期：8 轮用不满窗口，
修复几乎没生效；真正主导输入 token 的是**单轮里的工具往返次数**。详见第 13 周笔记第 3.4 节。

本周最重要的三条判断方法：**检查的位置有时比有无更重要**（T1）、**清理逻辑失败要有独立观测手段，
不能靠测试绿灯倒推**（T2）、**"从配置里拿走密钥"不等于"密钥不外泄"**（T5）。

## 第二阶段：第 14 周（进行中，2026-09-18 起）

- [x] 装 pytest 9.1.1 / ruff 0.16.8；
- [x] `UNIT_CASES` 迁到 pytest（`tests/` + `conftest.py`，探针走 `tmp_path`，迁移 21 条零 skip）；
- [x] 模型评估 3 → 10 条并按四类标注（有答案 4 / 无答案 2 / 跨文档 2 / 需要多跳 2），
      判据升级为"引用集合"，另加离线判据自检（`pytest` 共 **40 条**全绿）；
- [ ] 结构重构（`tools.py` 抽 `_iter_notes()`、周脚本抽 `_common.py`）；
- [ ] `ruff check` + `ruff format` 全量清理（现状 19 个问题）；
- [ ] 交叉验证两条测试入口（`pytest` 40 条 vs `eval_cases.py --unit-only` 12/12）。

**模型评估首跑（2026-09-18）**：`python eval_cases.py` → **20/22**
（单元 12/12；模型 8/10：有答案 4/4、无答案 2/2、跨文档 1/2、需要多跳 1/2）。

两条 FAIL（C1 跨文档、D2 多跳）的根因是**引用格式漂移**：模型把出处写成
`` `文件.md:行号` ``（反引号）而不是约定的 `[文件:行号]`，内容其实找对了。
**所以本轮分数不能当能力基线**——跨文档/多跳的 1/2 里混着格式问题。
处置方案（判据拆成"引用内容"+ "格式合规"两条、并保存完整回答）见台账 T10，
是下次开工的第一件事。

本轮用量：10 条合计输入 197,928 / 输出 19,409 token，49 次 API 请求；
单条成本与"工具往返次数"强相关（最贵的 C1 跑了 6 次请求、40,639 输入，
最便宜的 A2 只跑 3 次、3,971 输入，差 10 倍）。逐条数据见台账 T10。

## 文档校正记录（2026-09-15）

按仓库实际情况校正了一批过期与自相矛盾的内容：

- `学习Agent规划.md`：回填第 1 周与第 8~12 周的勾选、把"Python 3.10+"改为实际要求、修正目录名、补填"我的背景与期望"；
- `AGENTS.md`：重写目录结构、补"常用命令"与"不要做的事"；
- `README.md`：修正笔记范围（第 4~12 周）、Python 版本、文件清单；
- `笔记索引.md`：补上第 12 周笔记（原写"待补"）；
- `notes-qa/项目设计.md`：修正 `list_notes` 输出格式、回填第 9 周复选框、说明 U1 实际由后缀检查拦下；
- `第7/9/10/11/12周笔记.md`：给时效性内容和历史数字加"当时"标注；
- `第12周演示稿.md`：补完缺失的第 5、6 部分与备用问答；
- `requirements.txt`：按 `.venv` 实际版本重新对齐（openai 3.8.0 / openai-agents 0.22.0 /
  python-dotenv 1.2.3 / requests 2.34.2），修正过期的目录名与 Python 版本说明；
- **补写 4 篇缺失笔记**：`第1周笔记.md`、`第2周笔记.md`、`第3周笔记.md`、`第8周笔记.md`
  （至此 12 周每周都有独立笔记，共 12 篇；此前的"9 篇（第 4~12 周）"为误计，实际只有 8 篇，缺第 8 周）。

仍待处理（需改代码，本轮未动）：`search_notes` 缺文件大小限制、`main.py` 的 history 无上限、
评估探针残留（`_probe_linkdir.md`，根因是用 `os.rmdir` 删符号链接）等，清单见 `学习评审报告.md` 末节。

## 环境/代码注意事项

- Python 版本要求 **≥3.12**：`notes-qa/main.py` 用了 `asyncio.timeout`（需 3.11+），`notes-qa/eval_cases.py` 用了 `Path.is_junction()`（需 3.12+）。文档早期写的"Python 3.10+"是错的，本机实际使用 3.14.4。
- API Key 放在 `agent-learning/.env`，格式为 `DEEPSEEK_API_KEY=sk-...`；`.env` 已加入 `.gitignore`。
- 新电脑克隆仓库后，如果 `.venv` 不随 Git 同步（正常不会同步），需要重建环境：

  ```bash
  uv venv .venv
  uv pip install --python .venv/bin/python openai-agents python-dotenv
  ```

  （`openai` 会作为 `openai-agents` 的依赖自动安装。）
- 模型统一用 `deepseek-flash`，不要写 `gpt-*` 模型名。
- 遇到“没有找到 DEEPSEEK_API_KEY”：确认 `agent-learning/.env` 存在且内容格式正确；
  若仍报错，检查是否安装 `python-dotenv`。
- 遇到 `ModuleNotFoundError: No module named 'agents'`：安装 `openai-agents` 后再运行。
- 运行脚本前先验证环境：

  ```bash
  cd 仓库目录
  python agent-learning/00_first_api_call.py
  python agent-learning/01_hello_agent.py
  python agent-learning/02_agent_with_tool.py
  python agent-learning/03_multiturn_memory.py
  python agent-learning/04_multi_agent_handoff.py
  ```

## 仓库状态

- GitHub 远端：`https://github.com/Gold-pps/python-learning.git`（origin，HTTPS 方式）
- 当前分支：`main`
- 最近提交：见仓库里 `git log --oneline -1`
- 工作区状态（2026-09-15 时点）：文档校正与笔记补写已提交；另有残留的 `agent/` 目录（内含重复的 `.env`）待清理
- 仓库体积：`.git` 约 93MB，主要是 4 个 PPT 文件（`毕业答辩-*.pptx`、`组会汇报*.pptx`）的历史版本，与学习内容无关
