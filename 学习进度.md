# 学习进度

> 最后更新：2026-09-08
> 用途：在另一台电脑继续学习时，先读本文件恢复上下文。
> 路径说明：下文历史记录中的 `agent/` 目录现已重命名为 `agent学习/`。

## 当前位置

学习 Agent 开发，后端使用 **DeepSeek API**，开发框架用 **OpenAI Agents SDK**。所有请求都发往 `https://api.deepseek.com`，不需要 OpenAI 充值。

当前进度：**第 1～5 周已完成，第 6 周“安全与护栏”进行中**。

## 已完成

### 第 1 周：概念与环境

- 已注册 DeepSeek 平台并配置 API Key（存放在 `agent/.env`，已被 `.gitignore` 忽略，**不要提交到 GitHub**）。
- 本机 Python 环境位于仓库根目录 `.venv`，已安装：
  - `openai`
  - `openai-agents`
  - `python-dotenv`

### 第 2 周：跑通第一个 Agent

- [x] `agent/00_first_api_call.py`：普通聊天调用跑通，能正常返回 DeepSeek 回答。
- [x] `agent/01_hello_agent.py`：最小 Agent 跑通，正确回答“罗马帝国何时灭亡”。
- [x] 理解关键概念：`AsyncOpenAI(base_url=...)`、`set_default_openai_api("chat_completions")`、`Agent`、`Runner.run`、`final_output`。

接入 DeepSeek 的关键配置（三个脚本通用）：

```python
client = AsyncOpenAI(api_key=..., base_url="https://api.deepseek.com")
set_default_openai_client(client=client, use_for_tracing=False)
set_default_openai_api("chat_completions")
set_tracing_disabled(disabled=True)
```

### 第 3 周：让 Agent 学会使用工具

- [x] `agent/02_agent_with_tool.py` 已跑通工具调用循环。
- [x] 已注册 3 个 `@function_tool` 工具：
  - `get_beijing_time()`：返回当前北京时间；
  - `count_students_rows()`：统计 `students.csv` 数据行数（不含表头），当前文件为 11 行；
  - `find_student_home(name)`：按姓名在 `students.csv` 中查找家乡，带参数工具示例。
- [x] 观察到“模型决定调用工具 → Python 执行 → 结果返回模型 → 组织回答”的完整循环。
- [x] 理解工具说明书的 JSON Schema 长什么样（参数名、类型、required、additionalProperties）。
- [x] 使用 Chat Completions 兼容路径，未使用 Responses 专属工具能力。

已验证示例输出：

```text
[工具被调用] find_student_home(name=cain)
根据查询结果，Cain 的家乡是 Bible。
```

### 第 4 周：理解运行循环与状态

- [x] 阅读 OpenAI Agents SDK 的 Running agents 文档。
- [x] 掌握多轮对话如何保留上下文（history / session / continuation）。
- [x] 不使用 OpenAI 专属的服务端 continuation，改为手动传 `history`。
- [x] 做小练习：连续提问，Agent 能记住之前的信息。

产出：

- `agent/03_multiturn_memory.py`：手动传 history 的多轮记忆 Demo（已跑通）。
- `agent/03_no_history_test.py`：不传 history 的对照实验（模型答不出）。
- `agent/第4周笔记.md`：三种续接方式区别与实验记录。

### 第 5 周：多 Agent 编排与交接

- [x] 阅读 OpenAI Agents SDK 的 Orchestration and handoffs 文档。
- [x] 创建总控 Agent + 两个专长 Agent（历史老师、数学老师）。
- [x] 总控能根据问题类型自动交接，`result.last_agent` 可确认实际回答者。
- [x] 了解 Agent as tool 与 handoffs 的适用区别。

产出：

- `agent/04_multi_agent_handoff.py`：总控分流 + handoffs Demo（已跑通）。
- `agent/第5周笔记.md`：交接机制、踩坑记录与对照总结。

### 第 6 周：安全与护栏（进行中）

- [x] 阅读 OpenAI Agents SDK 的 Guardrails and human review 官方文档。
- [x] `agent学习/05_guardrails.py` 已创建并验证：
  - 输入护栏 `block_dangerous_operations` 能拦截危险操作请求；
  - 输出护栏 `block_secret_leak` 能拦截疑似 API Key 泄露。
- [x] `agent学习/06_human_approval.py` 已创建并验证：
  - 敏感工具 `delete_file` 使用 `needs_approval=True`；
  - `--decision reject` 和 `--decision approve` 两条路径均已跑通。
- [ ] 你自己运行这两个脚本，观察输出，并填写 `agent学习/第6周笔记.md` 的“我的复盘”部分。

产出：

- `agent学习/05_guardrails.py`：输入护栏 + 输出护栏 Demo；
- `agent学习/06_human_approval.py`：敏感操作审批 Demo；
- `agent学习/第6周笔记.md`：安全设计与实验记录。

## 下一步

1. 在 `agent学习` 目录运行：

   ```bash
   python 05_guardrails.py
   python 06_human_approval.py --decision reject
   python 06_human_approval.py --decision approve
   ```

2. 把观察结果填进 `agent学习/第6周笔记.md` 的“我的复盘”。
3. 完成后进入第 7 周“追踪与评估”：用本地日志定位问题、查看 DeepSeek 账单控制成本，
   并为前几周的 Agent 写评估用例。

## 环境/代码注意事项

- API Key 放在 `agent/.env`，格式为 `DEEPSEEK_API_KEY=sk-...`；`.env` 已加入 `.gitignore`。
- 新电脑克隆仓库后，如果 `.venv` 不随 Git 同步（正常不会同步），需要重建环境：

  ```bash
  uv venv .venv
  uv pip install --python .venv/bin/python openai-agents python-dotenv
  ```

  （`openai` 会作为 `openai-agents` 的依赖自动安装。）
- 模型统一用 `deepseek-v4-flash`，不要写 `gpt-*` 模型名。
- 遇到“没有找到 DEEPSEEK_API_KEY”：确认 `agent/.env` 存在且内容格式正确；
  若仍报错，检查是否安装 `python-dotenv`。
- 遇到 `ModuleNotFoundError: No module named 'agents'`：安装 `openai-agents` 后再运行。
- 运行脚本前先验证环境：

  ```bash
  cd 仓库目录
  python agent/00_first_api_call.py
  python agent/01_hello_agent.py
  python agent/02_agent_with_tool.py
  python agent/03_multiturn_memory.py
  python agent/04_multi_agent_handoff.py
  ```

## 仓库状态

- GitHub 远端：`git@github.com:Gold-pps/python-.git`
- 当前分支：`main`
- 最近提交：在仓库里执行 `git log --oneline -1` 查看
